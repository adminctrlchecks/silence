# Phase 2 — Implementation Progress & Loop State

> **Read this file first, every time you resume work.** It is the single source of truth for what is built, what is next, and how to keep going. Update it after every task you complete — before moving to the next one. This is the Phase 2 counterpart to `PROGRESS.md` (Phase 1, documentation, already complete).

---

## 0a. Current State (read this before anything else)

`main` is at commit `7118089` (docs updated further to `3dd8865`) and is **live in production** on both `https://silence-web-53h7.vercel.app` (Vercel, auto-deploys `main`) and `https://silence.ctrlchecks.ai` (Hostinger VPS, deployed via `deploy/deploy.sh`, kept in sync as of this commit). It contains: all of Phase 0-2, Phase 3 except the favicon/OG-image task, and Phase 4's Google OAuth item (built as client-side Google Identity Services, not the redirect flow originally sketched — see the Phase 4 checklist entry below for why).

This session works on branch `feat/product-redesign-continued` off that `main` (not pushed/merged — local commits only so far). It has completed Phase 3's remaining favicon/OG-image task (commit `eda4c28`, includes two incidental bug fixes: an i18n-proxy bypass for the new metadata routes, and a homepage `openGraph` override that was dropping `og:image`), all of Phase 4 (commit `3ed36e6`), all of Phase 5 (commit `2dec4db`), all of Phase 6 (commit `b1e9753`), all of Phase 7 (commit `bdb9174`), all of Phase 8 (commit `5a4135c`), and all of Phase 9 (commits `1c20a77`, `8d5a758`). Everything from Phase 10 onward in the Work Queue (§3) is genuinely not started. Continue with Phase 10 — the final phase; the roadmap's Definition of Done is satisfied once it's checked off and green. Note: this sandbox has no live Postgres+API stack, so authenticated-state UI (dashboard states, chart/remedy/history/profile/admin rendering, e2e suite) has been verified by code review + structural dev-server checks (redirect-when-signed-out, build output) rather than a live authenticated render — flag this for a manual click-through before merging. `/terms` and `/privacy` are real but explicitly pre-legal-review drafts (visible on-page notice) — do not treat as final, and open-decisions.md #6 (support/privacy contact channel) still blocks naming a real contact there.

## 0. Governing Spec

Phase 1 produced a complete blueprint in `docs/product-redesign/`. Phase 2 turns it into real, working code. The authority documents, in the order you need them:

1. **[33-implementation-roadmap.md](33-implementation-roadmap.md)** — the phase plan this file's checklist is built from.
2. **[32-priority-matrix.md](32-priority-matrix.md)** — P0/P1/P2/P3 ranking; P0 must be fully done before anything is called production-ready.
3. **[open-decisions.md](open-decisions.md)** — every `NEEDS DECISION` item has a **recommended default**. Where a task below is blocked by one, apply that default and keep moving. Do not stop to ask.
4. **The 30+ spec files (06–31)** — each covers one concern (navigation, color, typography, components, page specs, a11y, i18n, responsive, SEO, performance, security, etc.). Read the relevant spec file(s) before implementing the matching task — they contain the actual acceptance detail this checklist doesn't repeat.
5. **[34-qa-acceptance-criteria.md](34-qa-acceptance-criteria.md)** — testable criteria; use it as your verification checklist per page/flow.
6. **[35-final-implementation-checklist.md](35-final-implementation-checklist.md)** and **[36-final-gap-analysis.md](36-final-gap-analysis.md)** — what "done" looks like.

## 1. Hard Constraints (do not violate)

0. **`main` is live in two places now — pushing to it is a deploy, not a safe checkpoint.** Vercel auto-deploys `main` on every push (confirmed: pushes during this project have gone live on `silence-web-53h7.vercel.app` within ~1-2 minutes, no manual step). The VPS (`silence.ctrlchecks.ai`) needs an explicit `deploy/deploy.sh` run over SSH to pick up `main`, so it lags until that's run. Both `feat/product-redesign-v2` (Phases 0-3 + SEO) and `feat/google-sign-in` (Phase 4's Google OAuth item) are already merged into `main` and live on both. **Do not push to `origin/main` during this loop.** Work on a fresh branch per session (e.g. `feat/product-redesign-continued`), commit there, push that branch only. Merging to `main` (which is also "go live on Vercel") and running the VPS deploy are the user's call to make explicitly — stop and ask before either, the same way earlier sessions in this project did, rather than folding it into the autonomous loop.
1. **Work on a dedicated branch off current `main`.** Create it first if it doesn't already exist. Commit after each completed, verified task (small, reviewable commits, not one giant diff at the end).
2. **Never merge to `main`, push to `main`, deploy, touch anything under `deploy/` in a way that runs it, or run destructive database commands** without the user's explicit go-ahead in that session. Local commits (and pushing the *feature branch*, not `main`) are fine on your own.
3. **Never touch CtrlChecks**: no edits to `/opt/ctrlchecks-*`, no restart of its services, no reuse of its DB, no changes to shared infra outside this repo.
4. **Never expose or commit secrets.** `.env` files stay gitignored; never print their values into commits, comments, or generated code.
5. **Preserve existing business logic** (session lifecycle, remedy rule engine, auth, Prisma schema, API contracts) **unless a spec file explicitly calls for a change.** This is a UX/UI/IA redesign built on the current backend, not a rewrite of it. If a spec genuinely requires a backend change (e.g., new field), implement it precisely as scoped in the spec — nothing broader.
6. **Apply the recommended default from `open-decisions.md`** for anything tagged `NEEDS DECISION` rather than fabricating final content or stalling. In particular:
   - Legal pages (Terms/Privacy/Cookies): implement full page structure and real, product-accurate content per `19-legal-pages.md`, but mark the copy blocks clearly as `<!-- NEEDS LEGAL REVIEW -->` where the doc calls for reviewed legal language — do not fabricate binding legal terms.
   - No testimonials, user counts, logos, awards, or certifications anywhere — ever.
   - Business model: build for the free/admin-operated model. Do not add billing, pricing, paywalls, or usage limits.
   - Admin UI stays English-only for this pass (per decision #10) — do not spend time localizing admin components.
   - **Google OAuth is IN SCOPE for Phase 2's authentication work**, not deferred: `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` / `GOOGLE_CALLBACK_URL` already exist in `apps/api/.env` (real Google Cloud OAuth credentials, already created by the user). Implement it end-to-end (passport-google-oauth20 strategy + controller routes + `googleId` on the `User` model + "Continue with Google" UI) as part of Phase 4 — do not ship a fake/disabled button, and do not skip it citing the open-decision as unresolved; the decision is resolved by this instruction.
7. **Keep the app buildable and green at every checkpoint.** After each task: run typecheck, lint, and build for whatever workspace(s) you touched (`pnpm --filter @silence/web typecheck`, `pnpm --filter @silence/api typecheck`, `pnpm build`, `pnpm lint`, and relevant tests). Never move to the next task with a red build.
8. **Do not stop to ask the user questions.** If you hit something genuinely unresolvable (not covered by any spec file or open-decision default), make the most conservative, spec-consistent choice, note it in this file's log, and continue.
9. **Do not silently skip a P0 item.** If a P0 task turns out to be larger than expected, break it into sub-tasks in §3 rather than deferring it past P1/P2 work.

## 2. Completed Tasks Log

*(Append one row per completed task as you go — file(s) touched, what changed, how it was verified, commit hash.)*

| Phase | Task | Files touched | Verified via | Commit |
|-------|------|----------------|---------------|--------|
| Phase 0 | Create branch `feat/product-redesign-v2` from current `main` | `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `git branch --show-current` | `1c3a982` |
| Phase 0 | Re-read roadmap, priority matrix, and open decisions | `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `Get-Content -Raw` for all three planning docs | `bd90466` |
| Phase 0 | Confirm task breakdown matches specs | `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `Test-Path` check for cited spec files | `b0c1d3e` |
| Phase 1 | Implement color/status/destructive tokens | `apps/web/src/app/globals.css`, `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; Playwright smoke screenshots for `/` light/dark desktop and `/ar` mobile RTL | `a6c0c5a` |
| Phase 1 | Implement typography/spacing/container/radius/shadow tokens | `apps/web/src/app/globals.css`, `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; Playwright smoke screenshots for `/` light/dark desktop and `/ar` mobile RTL | `8cedc4a` |
| Phase 1 | Expand `components/ui` form, feedback, and navigation primitives | `apps/web/src/components/ui/button.tsx`, `apps/web/src/components/ui/input.tsx`, `apps/web/src/components/ui/textarea.tsx`, `apps/web/src/components/ui/select.tsx`, `apps/web/src/components/ui/checkbox.tsx`, `apps/web/src/components/ui/form-field.tsx`, `apps/web/src/components/ui/alert.tsx`, `apps/web/src/components/ui/skeleton.tsx`, `apps/web/src/components/ui/link.tsx`, `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; Playwright smoke screenshots for `/login` light/dark desktop and `/ar/login` mobile RTL | `be4d2e5` |
| Phase 1 | Add Dialog, Drawer, and Toast primitives | `apps/web/src/app/layout.tsx`, `apps/web/src/components/ui/dialog.tsx`, `apps/web/src/components/ui/drawer.tsx`, `apps/web/src/components/ui/toast.tsx`, `apps/web/src/components/ui/use-overlay-focus.ts`, `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; Playwright smoke screenshots for `/login` light/dark desktop and `/ar/login` mobile RTL | `c31d806` |
| Phase 1 | Verify light/dark contrast and RTL basics on primitives | `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | Token contrast audit script for light/dark semantic pairs; `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; Playwright smoke screenshots for `/login` light/dark desktop and `/ar/login` mobile RTL (`dir=rtl`) | `0d9e54d` |
| Phase 2 | Add public navbar, language switcher, mobile drawer, and footer legal links | `apps/web/src/app/(user)/layout.tsx`, `apps/web/src/components/navigation/nav-links.ts`, `apps/web/src/components/navigation/language-switcher.tsx`, `apps/web/src/components/navigation/public-navbar.tsx`, `apps/web/src/components/navigation/site-footer.tsx`, `apps/web/src/components/ui/dialog.tsx`, `apps/web/src/components/ui/drawer.tsx`, `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; Playwright smoke screenshots for `/` light/dark desktop and `/ar` mobile RTL, including mobile drawer Escape close | `f4c2cfc` |
| Phase 2 | Add authenticated user app navigation, mobile bottom nav, and profile drawer | `apps/web/src/app/(user)/layout.tsx`, `apps/web/src/components/navigation/public-navbar.tsx`, `apps/web/src/components/navigation/profile-menu.tsx`, `apps/web/src/components/navigation/user-app-navigation.tsx`, `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; Playwright smoke screenshots for authenticated `/` desktop/profile drawer and `/ar` mobile RTL with bottom nav | `b06e7bb` |
| Phase 2 | Verify accessible mobile drawer/menu behavior | `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | Playwright keyboard smoke for public mobile RTL drawer and authenticated profile drawer: focus starts inside, Tab remains trapped, Escape closes, focus restores; `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build` | `e067058` |
| Phase 2 | Add scoped 404/not-found pages for root, user/localized, and admin routes | `apps/web/src/components/not-found-content.tsx`, `apps/web/src/app/not-found.tsx`, `apps/web/src/app/(user)/not-found.tsx`, `apps/web/src/app/(user)/[...not-found]/page.tsx`, `apps/web/src/app/[locale]/(user)/not-found.tsx`, `apps/web/src/app/[locale]/(user)/[...not-found]/page.tsx`, `apps/web/src/app/(admin)/admin/not-found.tsx`, `apps/web/src/app/(admin)/admin/[...not-found]/page.tsx`, `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; Playwright smoke screenshots for `/not-real`, `/ar/not-real` RTL, and authenticated `/admin/not-real` | `c297648` |
| Phase 2 | Add shared page container/layout primitive across public, user, and admin shells | `apps/web/src/app/globals.css`, `apps/web/src/components/layout/page-container.tsx`, `apps/web/src/components/navigation/public-navbar.tsx`, `apps/web/src/components/navigation/site-footer.tsx`, `apps/web/src/components/navigation/user-app-navigation.tsx`, `apps/web/src/components/admin/admin-shell.tsx`, `apps/web/src/components/not-found-content.tsx`, `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; Playwright smoke screenshots for public `/`, authenticated `/ar` mobile RTL, and authenticated admin shell | `2c236bd` |
| Phase 3 | Replace the homepage with full public product story and start panel | `apps/web/src/app/(user)/page.tsx`, `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; Playwright smoke screenshots for `/` light/dark desktop and `/ar` mobile RTL; checked one `h1`, `#start`, and `#faq` | `b74e9fc` |
| Phase 3 | Satisfy how-it-works content on homepage instead of adding a duplicate page | `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | Homepage includes `#how-it-works` ordered 5-step section verified in previous Phase 3 smoke; no standalone route added because spec says only if not fully absorbed | `c109c34` |
| Phase 3 | Add per-page SEO metadata (title/description/OG/canonical) and admin `noindex` | `apps/web/src/app/layout.tsx`, `apps/web/src/app/(admin)/admin/layout.tsx`, every public/auth/user `page.tsx` under `(auth)`, `(admin-auth)`, `(user)`, plus their `[locale]/...` re-export counterparts, `docs/product-redesign/IMPLEMENTATION_PROGRESS.md` | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build` (full production build, 175 static pages generated, all `metadata` exports resolved without error) | `a3a7282` |
| Phase 4 | Implement Google OAuth end-to-end (done directly, not via the Codex loop; superseded the original passport-google-oauth20/redirect-flow plan with client-side Google Identity Services + ID-token verification, since the frontend/API are on different origins — see the file for the full reasoning) | `apps/api/src/integrations/google/*` (new), `apps/api/src/auth/auth.{controller,service}.ts`, `apps/api/src/chart/chart.service.ts`, `apps/api/src/users/users.service.ts`, `apps/api/src/common/dto.ts`, `apps/api/prisma/schema.prisma` + migration `20260817120000_add_user_google_id`, `packages/shared/src/schemas.ts`, `apps/web/src/components/auth/google-sign-in-button.tsx` (new), `apps/web/src/components/auth/auth-card.tsx`, `apps/web/src/components/profile/profile-details-card.tsx`, `apps/web/src/app/api/auth/google/route.ts` (new), `apps/web/src/lib/api.ts`, all 11 `apps/web/src/messages/*.json`, `deploy/deploy.sh` + env templates (VPS was missing `GOOGLE_CLIENT_ID` entirely — fixed) | `pnpm typecheck`/`lint`/`build` both workspaces; 5 new AuthService unit tests (create/reuse/link/reject-unverified) + full suite green (122 tests, 1 pre-existing DB-less e2e failure unrelated); live-verified on both Vercel and the VPS: button renders, light/dark, full Arabic RTL incl. Google's own button text, and a real click reaching Google's OAuth handler with correct client_id/origin | `1260c38` (merged to `main`, deployed to VPS) |
| Phase 4 | Fix: Google button stuck as a permanent loading skeleton on first load | `apps/web/src/components/auth/google-sign-in-button.tsx` | Manual repro on live Vercel deploy; added self-retry if `window.google` isn't attached yet when the script's onLoad fires, plus try/catch surfacing failures through the existing error banner instead of a silently-dead skeleton | `d83f3ac` (merged to `main`, deployed to VPS) |
| Phase 4 | Fix: Google button infinite teardown/rebuild loop preventing any click from landing | `apps/web/src/components/auth/google-sign-in-button.tsx` | Root-caused via `MutationObserver` on the live Vercel page: 8 DOM rebuilds/1.5s from a `ResizeObserver` watching the exact element `render()` clears and rebuilds (self-referential feedback loop). Fixed by observing a separate stable wrapper element + a width-change guard. Confirmed 0 rebuilds after the fix and a scripted click reaching Google's handler (previously threw a stale-element error) | `7118089` (merged to `main`, deployed to VPS) |
| Phase 3 | Add generated favicon, app icons, OG image, web manifest (`apps/web/public` stays empty by design — assets are generated via Next's `next/og` `ImageResponse` metadata-file conventions, not static files) | `apps/web/src/lib/brand-mark.tsx` (new, shared teal-circle/white-S/star mark), `apps/web/src/app/icon.tsx`, `apple-icon.tsx`, `opengraph-image.tsx`, `manifest.ts`, `manifest-icon/[size]/route.tsx` (all new), `apps/web/src/proxy.ts` (bypass fix — the i18n middleware was rewriting these dot-less root routes under a locale prefix and serving HTML instead of the image), `apps/web/src/app/(user)/page.tsx` (removed a redundant page-level `openGraph` override that was replacing, not merging with, the root layout's resolved `openGraph` object and silently dropping `og:image`) | `pnpm --filter @silence/web typecheck`; `pnpm --filter @silence/web lint`; `pnpm --filter @silence/web build`; dev-server fetch checks confirming `/icon/16`, `/icon/32`, `/apple-icon`, `/opengraph-image`, `/manifest-icon/192(+maskable)`, `/manifest-icon/512(+maskable)` return `image/png` with no redirect, `/manifest.webmanifest` returns `application/manifest+json`, and the homepage `<head>` includes `og:image`/`twitter:image` alongside `icon`/`apple-touch-icon`/`manifest` `<link>` tags | `eda4c28` |
| Phase 4 | Progressive 3-step `/register`, Terms/Privacy links + consent, login/forgot/reset hierarchy, `/profile/security` split | `apps/web/src/components/auth/auth-card.tsx` (3-step rewrite), `reset-password-card.tsx` (+`forgotPasswordHref` prop, "Request a new reset link" CTA), `apps/web/src/app/(user)/profile/page.tsx` + new `profile/security/page.tsx` (+ `[locale]` re-export), `(auth)/reset-password/page.tsx`, `(admin-auth)/admin/reset-password/page.tsx`, all 11 `apps/web/src/messages/*.json`, `apps/web/e2e/user-journey.spec.ts` (updated to click through the 3 register steps) | `pnpm --filter @silence/web typecheck`/`lint`/`build`; manual dev-server walkthrough of `/register` in Arabic RTL (per-step validation incl. custom place-not-selected guard, focus-to-heading, step indicator, Terms/Privacy links resolving), `/login` in English (new eyebrow/description/agreement-notice), `/reset-password` invalid-link CTA, `/profile` + `/profile/security` redirect-when-signed-out. Full Playwright e2e suite NOT run (no live Postgres+API stack in this sandbox) — run `pnpm test:e2e` before merging | `3ed36e6` |
| Phase 5 | Redesign `/app` around next-action + journey tracker; remove quick-links card grid (nav already covers it); fix profile-completeness bar's missing `role=progressbar` a11y attrs | `apps/web/src/components/dashboard/journey-tracker.tsx` (new), `apps/web/src/app/(user)/app/page.tsx` (rewrite), all 11 `apps/web/src/messages/*.json` | `pnpm --filter @silence/web typecheck`/`lint`/`build`; confirmed `/app` still redirects to `/login` signed-out. The four authenticated dashboard states (first-time/in-progress/chart-ready/remedy-ready/returning) were verified by code review against the `DashboardSummary` shape, not a live render — no Postgres+API stack in this sandbox (same constraint as Phase 4's log entry). Recommend an authenticated click-through before merging | `2dec4db` |
| Phase 6 | Question flow focus/save-state/scrollable tabs/sticky mobile action; chart hierarchy reorder (interpretation before technical tables) + a11y (aria-hidden chart + text summary, table `scope`) + ~25 hardcoded strings translated; remedy restructured into what/why/how-to-repeat + completion CTAs; shared `ReadingDisclaimer` (open-decisions.md #5) added to chart+remedy; history gets real pagination + timeline styling + richer empty state; history detail gets icon'd empty states | `apps/web/src/components/questions/question-flow.tsx`, `apps/web/src/components/chart/birth-chart-view.tsx` (rewrite), `apps/web/src/components/shared/reading-disclaimer.tsx` (new), `apps/web/src/app/(user)/app/remedy/page.tsx` (rewrite), `apps/web/src/app/(user)/history/page.tsx` (rewrite), `apps/web/src/app/(user)/history/[id]/page.tsx`, all 11 `apps/web/src/messages/*.json` | `pnpm --filter @silence/web typecheck`/`lint`/`build`; confirmed `/app/questions`, `/app/chart`, `/app/remedy`, `/history`, `/history/[id]` all still redirect to `/login?redirect=...` signed-out. Did not visually verify authenticated states — no Postgres+API stack in this sandbox (same constraint as Phases 4-5) | `b1e9753` |
| Phase 7 | Split `/profile` into Overview/Birth Details/Security/Privacy routes (shared `ProfileNav`); new `/profile/privacy` (consent status, what-we-store, AI-processing disclosure, Terms/Privacy links, manual data-request note per open-decisions.md #3, deliberately no invented contact channel — #6 unresolved); new `/admin/settings` (admin password change moved off `/admin` dashboard + audit-log link) | `apps/web/src/components/profile/profile-nav.tsx`, `profile-overview-card.tsx` (new), `birth-details-card.tsx` (renamed/rewritten from `profile-details-card.tsx`), `apps/web/src/app/(user)/profile/page.tsx`, `birth-details/page.tsx` (new), `privacy/page.tsx` (new), `security/page.tsx` (+ `[locale]` re-exports), `apps/web/src/app/(admin)/admin/settings/page.tsx` (new), `(admin)/admin/page.tsx`, `apps/web/src/components/admin/admin-sidebar.tsx`, `apps/web/src/components/auth/auth-card.tsx` (Google onboarding redirect now targets birth-details), all 11 `apps/web/src/messages/*.json` | `pnpm --filter @silence/web typecheck`/`lint`/`build`; confirmed `/profile`, `/profile/birth-details`, `/profile/privacy`, `/profile/security`, `/admin/settings` all redirect to login signed-out. Did not visually verify authenticated states — no Postgres+API stack in this sandbox (same constraint as Phases 4-6) | `bdb9174` |
| Phase 8 | New `/terms` (12 sections) and `/privacy` (10 sections incl. real Cookies section) with shared `LegalPageLayout`; visible draft/NEEDS-LEGAL-REVIEW notices; English-only body copy with a translated notice explaining why; disclaimer/FAQ links now point at real `#disclaimer`/`#ai-processing` anchors | `apps/web/src/components/legal/legal-page-layout.tsx` (new), `apps/web/src/app/(user)/terms/page.tsx`, `privacy/page.tsx` (new, + `[locale]` re-exports), `apps/web/src/app/(user)/page.tsx` (FAQ links), `apps/web/src/components/shared/reading-disclaimer.tsx` (anchor link), all 11 `apps/web/src/messages/*.json` | `pnpm --filter @silence/web typecheck`/`lint`/`build`; dev-server checks: full section/TOC text confirmed on `/terms` and `/ar/privacy` (chrome translated, body English + notice), footer/disclaimer links to `/terms`/`/privacy` return 200 (previously 404) | `5a4135c` |
| Cross-cutting (user-reported) | End-to-end i18n audit and fix, prompted by explicit user feedback mid-session that switching language should change everything, not just headers. Found and fixed: homepage ((user)/page.tsx) was ~90% hardcoded English despite calling getTranslations — only 2 of ~65 strings actually used it; global chrome (PublicNavbar/SiteFooter/ProfileMenu/UserAppNavigation/SignOutButton) had never been wired to next-intl since Phase 2; ProfileMenu's Security/Privacy links were silently broken since Phase 7 (still pointed at `/profile#security`/`#privacy` anchors instead of the real routes); and — the actual root complaint — authenticated users' question/chart/remedy content follows `profile.lang`, independent of the page-locale switcher, so switching language changed labels but not reading content. Fixed by having LanguageSwitcher also PUT the new language to `/api/profile` when signed in. Also removed 6 dead pre-Phase-3 `Home.*` keys, one of which (`savedProfileValue: "JWT"`) was exactly the "don't leak JWT into user copy" violation the auth spec flags. Also fixed a `not-found.tsx` double-suffixed browser-tab-title bug found while verifying | `apps/web/src/app/(user)/page.tsx` (full i18n rewrite), `apps/web/src/components/navigation/{public-navbar,site-footer,profile-menu,user-app-navigation,language-switcher}.tsx`, `apps/web/src/components/auth/sign-out-button.tsx`, `apps/web/src/app/(user)/layout.tsx`, `apps/web/src/app/not-found.tsx` + `(user)/not-found.tsx` (title fix), all 11 `apps/web/src/messages/*.json` (new `Home.*`/`Nav.*`/`NotFound.*`/`UserLayout.signOut(ing)` keys) | `pnpm --filter @silence/web typecheck`/`lint`/`build`; dev-server verified after a full restart (Next.js caches locale bundles per-process — mid-session JSON edits need a restart, not just HMR): `/fr` homepage fully French incl. interpolated step numbers and FAQ rich-text links; `/ar` footer fully Arabic incl. dynamic copyright year; fresh-tab console shows zero errors. Did not verify the authenticated profile-lang-sync PUT against a live session — no Postgres+API stack in this sandbox | `1f5a673` |
| Phase 9 | Pagination (20/page) for `/admin/questions`/`answers`/`remedies`; `ConfirmDialog` replacing all 3 `window.confirm()` calls; AI-review "N awaiting review" badge; import-job status gets icon/color per state + destructive highlight on nonzero errors; user-reported addition: saved answers now show the question text they answered (was level-only) on both `/history/[id]` and admin's per-user session view, via a new backend `SessionsService.getDetail()` batched question lookup + `SavedUserResponse.questionText` field; mobile filter/search verified already-responsive, no changes needed | `apps/web/src/components/ui/confirm-dialog.tsx` (new), `apps/web/src/components/admin/{questions,answers,remedies,import}-admin.tsx`, `apps/web/src/components/admin/users-admin.tsx`, `apps/web/src/app/(user)/history/[id]/page.tsx`, `apps/api/src/sessions/sessions.service.ts` (+3 new unit tests), `packages/shared/src/types.ts` | `pnpm --filter @silence/shared build` (dist consumed by web/api, needed rebuilding for the new type field) + `typecheck`/`lint` for shared/web/api all green; `pnpm --filter @silence/api build`/`pnpm --filter @silence/web build` both green; `pnpm --filter @silence/api test -- sessions`: 21/21 passing incl. 3 new `getDetail()` cases | `1c20a77`, `8d5a758` |

## 3. Work Queue (in roadmap order — P0 items are distributed across phases 1–6 and 8; do not reorder across phases, but within a phase prefer P0 tasks first)

### Phase 0 — Decisions & Setup
- [x] Create branch `feat/product-redesign-v2` from current `main`.
- [x] Re-read `33-implementation-roadmap.md`, `32-priority-matrix.md`, `open-decisions.md` in full.
- [x] Confirm this file's task breakdown still matches the specs (specs are the source of truth if they've been edited since this file was written); adjust checklist rows below if needed.

### Phase 1 — Design System `P0`
- [x] Implement color/status/destructive tokens per [09-color-system.md](09-color-system.md) and [12-design-tokens.md](12-design-tokens.md) in `apps/web/src/app/globals.css`.
- [x] Implement typography/spacing/container/radius/shadow tokens per [10-typography-system.md](10-typography-system.md), [11-spacing-grid-layout.md](11-spacing-grid-layout.md), [12-design-tokens.md](12-design-tokens.md).
- [x] Expand `components/ui/`: Button variants, Link, FormField, Input, Textarea, Select, Checkbox, Alert, Skeleton per [13-component-system.md](13-component-system.md).
- [x] Add Dialog/Drawer/Toast primitives (needed by later phases — admin confirm dialogs, mobile nav drawer, form feedback).
- [x] Verify light/dark contrast (WCAG AA) and RTL basics on the new primitives per [21-accessibility.md](21-accessibility.md).

### Phase 2 — Global Shell `P0` (mobile nav), `P1` (rest)
- [x] Public navbar + footer per [07-navigation-architecture.md](07-navigation-architecture.md) — footer must include legal links (P0, since Terms/Privacy don't exist yet, link them now and build the pages in Phase 8).
- [x] User app navigation: mobile bottom nav / drawer, profile menu.
- [x] Accessible mobile drawer/menu (focus trap, ESC to close, `aria-*`).
- [x] Custom `not-found.tsx` (404) at the appropriate route-group levels.
- [x] Shared page container/layout components used consistently across public/user/admin shells.

### Phase 3 — Public Experience `P0`
- [x] Replace the `/` session-picker homepage per [15-homepage-specification.md](15-homepage-specification.md) — hero, product visual/demo, how-it-works, benefits, trust/privacy section, start panel, FAQ, final CTA, footer.
- [x] `/how-it-works` page if not fully absorbed into the homepage, per [14-page-specifications.md](14-page-specifications.md).
- [x] Per-page metadata/SEO (title, description, OG tags, canonical) per [28-seo.md](28-seo.md) — replace the single global `<title>`/`<meta description>`.
- [x] Favicon, app icon, OG image per [24-image-and-asset-strategy.md](24-image-and-asset-strategy.md) — `apps/web/public/` is currently empty; populate it. Use only licensed/owned sources (Unsplash/Pexels/Lucide licenses as already referenced in the Phase 1 docs) — never unlicensed stock art.

### Phase 4 — Authentication `P0` (registration restructuring, legal links), `P1` (rest)
- [x] Restructure `/register` into progressive steps with a birth-data "why we ask" explanation, per [18-authentication-flows.md](18-authentication-flows.md).
- [x] Add Terms/Privacy links and consent copy to registration.
- [x] Improve `/login`, `/forgot-password`, `/reset-password` visual hierarchy and states.
- [x] Move password-change UX out of `/profile` into the new settings/security area (coordinate with Phase 7) — landed as its own `/profile/security` route for now; Phase 7 should fold this into its Overview/Birth Details/Security/Privacy tab structure rather than redo it.
- [x] **Implement Google OAuth end-to-end** — done. Live on both Vercel and the VPS. Implemented as client-side Google Identity Services (ID-token verification via `POST /auth/user/google`), not the originally-planned passport-google-oauth20 redirect flow — the frontend and API are on different origins/domains, so a redirect flow would need a cross-domain cookie hop this design avoids. `GOOGLE_CLIENT_SECRET`/`GOOGLE_CALLBACK_URL` are provisioned but unused by this design (only `GOOGLE_CLIENT_ID` is needed). New Google sign-ups get blank birth details + `profileComplete: false` and are routed to `/profile?onboarding=1` instead of `/app` — this already covers part of Phase 7's "Privacy/data-handling" and profile-splitting intent for that one flow; worth checking against Phase 7's task when you get there so it isn't redone from scratch.

### Phase 5 — Application Shell & Dashboard `P0`
- [x] Redesign `/app` around next-action + journey tracker per [16-dashboard-specification.md](16-dashboard-specification.md).
- [x] Implement first-time / in-progress / returning / complete dashboard states.
- [x] Move the quick-links grid into nav/secondary region rather than the primary content area — removed the card grid outright since the Phase 2 app nav (top bar + mobile bottom nav) already covers Dashboard/Questions/Chart/Remedy/History.
- [x] Mobile-first dashboard layout.

### Phase 6 — Core Product Pages `P1`
- [x] Question flow: improved stepper, cards, save-state indicators, focus management per [14-page-specifications.md](14-page-specifications.md).
- [x] Chart: plain-language summary above the technical data, accuracy badge, accessible chart/table pairing, clear next-step CTA.
- [x] Remedy: structured practice card (what/why/how-often), "why selected" copy in plain language, completion state.
- [x] History + detail: visual timeline, pagination/load-more, better partial-data states.

### Phase 7 — Profile & Settings `P1`
- [x] Split `/profile` into Overview / Birth Details / Security / Privacy sections per [17-profile-settings-specification.md](17-profile-settings-specification.md).
- [x] Password change lives under Security.
- [x] Add a Privacy/data-handling section reflecting the manual-request model from `open-decisions.md` #3 (no self-serve export/delete claims).
- [x] Add an admin settings route (`/admin/settings`) for admin password/security, out of the dashboard page.

### Phase 8 — Legal & Trust `P0`
- [x] Implement `/terms` and `/privacy` page layout and structure per [19-legal-pages.md](19-legal-pages.md), with content marked for legal review where the spec calls for it.
- [x] Add the chart/remedy disclaimer copy (concise on-page note, per decision #5) and link to the fuller Terms section. (Built in Phase 6 as `ReadingDisclaimer`; this phase pointed it at the real `/terms#disclaimer` anchor now that the page exists.)
- [x] Add a Cookies section within Privacy (essential cookies only, per decision #14) — no separate tracking/consent banner since no analytics/marketing cookies are in scope.

### Phase 9 — Admin Quality & Operations `P1`
- [x] Add pagination to `/admin/questions`, `/admin/answers`, `/admin/remedies` (currently `limit=100`, no UI pagination — content silently truncates).
- [x] Replace all `window.confirm()` calls with the new Dialog primitive from Phase 1.
- [x] Improve AI-review queue, import-failure, and user-timeline views per [01-repository-audit.md](01-repository-audit.md) findings and [13-component-system.md](13-component-system.md). (AI-review: "N awaiting review" badge; import: per-status icon/color + errors-count highlight; user-timeline: saved answers now show the question text they answered, not just the level.)
- [x] Add mobile filter/search patterns to admin list views. (Verified — all four admin list views already collapse their filter grids to a single mobile column via existing responsive breakpoints from Phase 1/2; no dedicated mobile filter drawer was built beyond that.)

### Phase 10 — Accessibility, Responsive, Performance, SEO, QA `P0`/`P1` (this phase gates "done")
- [ ] WCAG 2.2 AA pass across all touched pages per [21-accessibility.md](21-accessibility.md).
- [ ] Mobile + RTL (Arabic) screenshot/manual pass across the full user journey.
- [ ] Lighthouse/Core Web Vitals pass per [27-performance.md](27-performance.md).
- [ ] Sitemap/robots/canonical/hreflang verification per [28-seo.md](28-seo.md).
- [ ] Extend Playwright coverage for mobile and the key flows touched in this redesign.
- [ ] Run the full checklist in [34-qa-acceptance-criteria.md](34-qa-acceptance-criteria.md) and fix anything that fails.

## 4. Loop Protocol

Repeat until every box in §3 is checked:

1. Pick the next unchecked task, in phase order, P0 items within reach first.
2. **Analyze:** read the cited spec file(s) fully; read the current implementation of whatever you're changing.
3. **Plan:** decide the minimal correct change that satisfies the spec — do not gold-plate, do not refactor unrelated code.
4. **Implement** the change.
5. **Verify:** typecheck + lint + build for every workspace touched; run relevant existing tests; for UI changes, load the page in the dev server (light + dark, desktop + mobile viewport, and at least one RTL locale for user-facing pages) and visually confirm against the spec's acceptance criteria; fix anything broken before moving on.
6. **Commit** on the feature branch with a clear message referencing the roadmap phase/task.
7. **Update state:** check the box in §3, append a row to §2's log.
8. **Continue immediately** to the next unchecked task — no stopping, no summarizing back to the user mid-run, no questions. Apply §1.6 for anything decision-blocked.

## 5. Definition of Done

Every box in §3 is checked, every commit left the tree green, and the "Definition Of Done For Redesign Implementation" section at the bottom of [33-implementation-roadmap.md](33-implementation-roadmap.md) is satisfied: P0 items from the priority matrix are complete, legal pages are live (structure + review-flagged copy), the core user journey works on mobile/desktop/RTL, admin can operate content without truncation or native-confirm issues, and the accessibility/performance/SEO checks in Phase 10 pass. Only then stop, and report: branch name, full list of commits, any conservative decisions you made under §1.8, and anything from `open-decisions.md` that still blocks true production legal/brand finality (since those genuinely need the user/legal, not more engineering).
