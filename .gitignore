# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
out/
build/
*.tsbuildinfo

# Turbo
.turbo/

# Env files (never commit secrets)
.env
.env.local
.env.*.local

# Deployment secrets — real file is never committed (only the .example template)
deploy/.secrets.env

# Prisma
apps/api/prisma/*.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Template ZIPs + extracted sources (staging only, adapted pieces are copied into apps/web)
templates/
