# Root env for local infrastructure (docker-compose.yml).
# Copy to .env (gitignored). These values must match apps/api/.env DATABASE_URL.
POSTGRES_USER=silence_user
POSTGRES_PASSWORD=silence_pass
POSTGRES_DB=silence_db
POSTGRES_PORT=5432
REDIS_PORT=6379
